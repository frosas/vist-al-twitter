### Usage

Copy `.env-example` to `.env` and edit it, or set its environment variables
(`export KEY=VALUE`).

```bash
$ npm install
$ DEBUG="*,-puppeteer:*" npm start
```

### Development

```bash
$ npm test
$ npm run lint
```